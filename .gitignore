# --- Windows / macOS мусор -------------------------------------------------
Thumbs.db
ehthumbs.db
desktop.ini
Desktop.ini
$RECYCLE.BIN/
.DS_Store
._*

# --- Редакторы и IDE -------------------------------------------------------
.vscode/
.idea/
*.swp
*~

# --- Локальные настройки Claude Code (пути с этого компьютера) --------------
.claude/

# --- Python ----------------------------------------------------------------
__pycache__/
*.pyc

# --- Node (если когда-нибудь появится) -------------------------------------
node_modules/

# --- Старая версия магазина: лежит на диске, но на сайт не публикуется ------
# Это бэкап дореформенного кода. Он сломан (ссылается на несуществующие файлы).
# Если он больше не нужен — просто удали папку CaptainShop/_legacy/.
CaptainShop/_legacy/

# --- Редактор ивентов: только локально, на сайт не публикуется --------------
# Лежит на диске и работает через локальный сервер (см. DOCS.md).
# ВАЖНО: в репозитории его нет — в свежем клоне этой папки не будет.
editor/

# --- Временные файлы -------------------------------------------------------
*.bak
*.log
*.tmp
